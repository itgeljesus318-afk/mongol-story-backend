const generateText = async (childName, storyChoice) => {
  // Simple placeholder story
  return `Once upon a time, ${childName} went on an adventure in the magical world of ${storyChoice}.`;
};

module.exports = generateText;
